#include "lists.h"

/**
* print_listint_safe - function that prints a listint_t linked list
* @head: the node head
* Return: the number of node in the list
* Description: 1 ->  2 -> 3
*                    |___ |
* This is a cycle we need to detect where that happend return the header that do that (3)
*/


size_t print_listint_safe(const listint_t *head)
{
	size_t size = 0;



	return (size);
}
