#include "main.h"
#include <string.h>
/**
 * _strlen - return the length of giving string
 * @s: string to check
 * Return: the length of the giving string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
