#include "main.h"

/**
 * _islower - function that checks for lowercase character
 * @c: character to test
 * Return: 1 if char in lowercase , 0 if not
 */

void puts2(char *str);
{
	if (c >= 97 && c <= 122)
		return (1);
	return (0);
}
