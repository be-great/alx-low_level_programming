#include "main.h"

/**
 * _islower - function that check if character is lower or not
 * @c: character to check
 * Return: 1 if true else 0
 */

int _islower(int c)
{
	return (c >='a' && c <= 'z');
}
