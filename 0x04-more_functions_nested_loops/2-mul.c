#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first number
 * @b: second number
 * Return: the result of multiplies
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
